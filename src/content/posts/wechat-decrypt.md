---
title: 微信数据库是怎么解密的？聊聊 wechat-decrypt 的实现原理
published: 2026-08-20
image: /images/wechat-decrypt/wechat-unlock.png
description: 原项目已因 DMCA 下架，记录其解密微信本地数据库的思路：从进程内存中的密钥，到 SQLCipher 页解密、HMAC 验证和 WAL 处理。
category: 开发实践
tags:
  - SQLite
  - macOS
  - 加密原理
draft: false
---

> 本文仅供学习交流，讨论的是自己或获授权账号的本地数据。原项目 wechat-decrypt 已因 DMCA 下架，本文不提供源码、下载地址或镜像，只记录我对其实现原理的理解。

## 缘由

最近做的 Funtree 项目里有个群聊分析功能，需要把微信聊天记录整理到后台，方便查找业务信息、做总结，以及回看原消息。

前面这些需求听起来还挺普通，真正开始接的时候，第一个问题就来了：微信聊天记录怎么读出来？

找到本地文件，发现是 `.db`，以为用 SQLite 打开就行。结果不是这么回事，它是加密的。

项目之前用到的 wechat-decrypt，是一个读取微信本地数据的工具。它从运行中的客户端寻找密钥，把加密数据库还原成可以查询的副本，再整理出聊天内容。现在原仓库已经下架，这篇把它的处理过程单独记下来，不需要先看过源码。

以下内容基于我保留的旧版本实现，主要对应 Mac 微信 4.x。当时运行环境是 Intel Mac、macOS 12.7.6、微信 4.1.8。微信会更新，本地存储和进程内存中的细节也会变，这里讲的是这版实现的思路，不是所有版本都适用的结论。

## 它没有在暴力破解密码

说到“微信解密”，很容易联想到拿个工具一直猜密码，或者利用什么算法漏洞，把加密给算开了。

这个项目不是这样做的。

先想一个很直观的问题：数据库在磁盘上是密文，但是微信登录以后，聊天记录是能正常显示的。也就是说，客户端在读取数据库时，自己就得具备解密能力。

那么，解密所需的密钥，在某个时刻就会出现在客户端的运行环境里。

原项目利用的就是这一点：**从本机运行中的微信进程中寻找数据库密钥，再按数据库的加密格式还原文件。**

这里的密钥，可以理解成加密算法使用的一串二进制数据。算法需要它，才能把密文还原成原来的内容。它不是微信登录密码；工具也没有向微信服务器请求聊天记录，更没有通过这个过程获取账号的登录权限。

所以“拿到一个加密数据库文件”和“有条件读取已登录客户端的进程内存”，是两件差别很大的事。只有前者，缺少密钥，这套思路并不能凭空把消息变出来。

整个过程可以先看这张图：

![从进程密钥到可查询微信数据库的解密流程](/images/wechat-decrypt/data-flow.svg)

## 第一步：先弄清楚读的是什么

微信的本地聊天数据，不是一个文件里装着所有东西。

联系人、会话列表、消息正文会分别存储，消息多了还会分成多个数据库。图片和视频等附件又有自己的文件和索引。

可以先把它理解成下面几类：

| 数据 | 主要作用 |
| --- | --- |
| 联系人数据库 | 把账号标识对应到昵称、备注等信息 |
| 会话数据库 | 记录有哪些聊天、最近消息和会话状态 |
| 消息数据库 | 存放消息正文、发送者标识、时间等，可能有多个分库 |
| WAL 文件 | 保存尚未合并回主数据库的页面更新 |
| 媒体索引和附件 | 把图片、语音、视频等消息对应到实际资源 |

SQLite 是一种嵌入式数据库。应用不需要另外连接一台数据库服务器，就能把表、索引和记录保存在本地文件中。

本文讨论的微信数据库使用 SQLite 的存储结构，又加了一层 SQLCipher 加密。SQLCipher 是带加密能力的 SQLite 实现：SQLite 负责组织数据，SQLCipher 在读写文件时处理加解密。后面说的“页”，就是数据库在文件里组织数据的一块固定大小的空间。

因此，直接用普通 SQLite 读取原文件，看到“不是数据库”之类的报错并不奇怪。连文件头和页面正文都不是它预期的内容，当然读不出来。

原项目首先要解决的，就是把这些加密页面还原成 SQLite 可以理解的页面。

## 第二步：在内存里找密钥

### 为什么可以找到

数据库被打开时，客户端需要把密钥交给加密层。这版微信在进程内存里留下了一种可识别的密钥表示，原项目针对这种表示做搜索。

它的结构大致是：一个带引号的十六进制字符串，里面包含 **32 字节的加密密钥和 16 字节的 salt**。换成十六进制文本，就是 64 个字符的 key，加上 32 个字符的 salt。

扫描器寻找的是这种格式特征，并不是把内存里任意 32 字节都拿来解一遍。

这个区别很重要。搜索已知格式的候选和穷举一个 256 bit 密钥，根本不是一个工作量。

“扫描器”就是原工具中负责搜索内存的那一部分。不过，这种格式不是微信对外承诺的接口。客户端换一种保存方式，旧扫描器就可能找不到。所以有时候工具升级要跟着微信升级跑，原因就在这里。

### Mac 上怎么读取进程

进程就是程序运行起来以后，操作系统管理的那个实例。它有自己的内存空间，客户端正在使用的数据、临时结果和密钥都可能出现在其中。

原项目的 macOS 版本通过系统提供的 Mach 接口访问这片内存。这里不用记具体接口名，只要知道它分成三个动作：取得访问目标进程的能力，遍历内存区域，再读取符合条件的区域。

它选择可读写的内存区域，分块扫描。相邻两块之间还留了一点重叠，避免目标字符串恰好被切在边界上。

这个细节挺好理解：前一块只有密钥的前半段，后一块只有后半段，如果各扫各的，就相当于两边都没找到完整内容。

当然，操作系统不会让普通程序随便读另一个程序的内存。运行权限、应用签名、系统保护状态都会影响这一步。**能不能读取进程，是密钥扫描的前置条件，不是 AES 算法能帮忙绕过去的事情。** 这里不展开修改系统或应用保护设置的操作。

## 第三步：这个 key 到底属于哪个库

内存里找到了候选，也不能立刻宣布成功。

一方面，内存中可能留着多个数据库、多个打开过程的相关内容；另一方面，微信本地本来就有多个库。总得知道哪个 key 对应哪个文件吧。

这里就轮到 salt 出场了。

### salt 不需要保密

Salt 通常翻译成“盐”，是一段参与密钥派生的附加数据。所谓派生，就是把已有材料按固定算法处理，得到后续需要的密钥；不同的 salt 能让派生过程产生不同结果。它本身不等于密钥，也不要求像密钥一样保密。

这一版加密数据库的开头 16 字节，就是数据库的 salt。扫描器从内存候选里拆出的 salt，可以和文件开头的值比较。

两边对上，就能建立“这个数据库，对应这组候选密钥”的关系。

所以它不是找到一个万能 key，再把所有文件挨个解一遍；而是要为数据库找到对应的密钥信息。不同文件也可能共用某些密钥材料，但不能因此省掉对应关系和验证。

### 对上 salt，还不算验证完成

Salt 匹配主要负责定位。

候选密钥能不能正确处理这个数据库，还要看后面的 HMAC 验证。可以先把 HMAC 理解成一份“用密钥算出来的校验结果”：读取时重新计算，再和文件里保存的值比较。具体怎么算，下一节再讲。

如果验证失败，可能是候选不对、文件状态不一致，也可能是读到的内容已经损坏。

把这两步分开，就比较好理解了：salt 帮忙找对象，HMAC 再检查拿到的内容和密钥能不能对上。

## 第四步：一页一页地解开

SQLite 不是把所有数据当作一整段文本存进去的，它按“页”组织文件。

这一版的页大小是 4096 字节。SQLCipher 加密也围绕页面进行，所以解密时不用把整个数据库塞进一次 AES 调用，而是逐页处理。

这里用到的 AES 是一种对称加密算法，加密和解密使用同一把密钥。AES-256 中的 256 指密钥长度是 256 bit，也就是 32 字节；CBC 则是它处理一组组数据时采用的工作模式。

先把几个数字放在这里，后面看图就不会绕。IV 和 HMAC 的作用接着解释，不用急着记缩写：

| 项目 | 本文对应格式 |
| --- | --- |
| 数据库页大小 | 4096 字节 |
| 加密算法 | AES-256-CBC |
| 加密密钥 | 32 字节 |
| 每页 IV | 16 字节 |
| 页 HMAC | HMAC-SHA512，64 字节 |
| 页尾保留区 | 80 字节，存 IV 和 HMAC |

第一页比较特殊，开头的 16 字节放了 salt；普通页没有这个部分。

![SQLCipher 第一页和普通页的字节布局](/images/wechat-decrypt/page-layout.svg)

于是第一页真正需要解密的正文是 4000 字节，其他页是 4016 字节。它们都刚好满足 AES 的 16 字节分组大小。

### IV 不是另一个密码

IV 是初始化向量，可以理解成 CBC 开始处理一页数据时使用的起始值。它让不同页面的加密过程有各自的起点，避免相同的明文开头在同一密钥下总是直接对应相同的密文开头。它和密钥的职责不同，不需要靠隐藏 IV 来保密。

同一个数据库的页面使用相应的数据库密钥，而每一页携带自己的 IV。还原这一页时，要取它自己的 IV，不能随便拿第一页的 IV 用到底。

从文件操作的角度看，解密过程就是：读一页，分出正文、IV 和 HMAC，验证并解密正文，再把结果放回正确的位置。

算法名称看着比较吓人，实际最容易写错的往往是这些位置和长度。

### HMAC 检查的是什么

AES 解密和 HMAC 验证是两件事。

错误的密钥也可能让 AES 解密函数返回一段字节。函数没有报错，不代表那些字节就是正确数据。

HMAC 是一种带密钥的消息认证码，这里的“消息”指参与计算的数据，不是微信群里的一条聊天。写入时，用数据和 MAC 密钥算出认证码；读取时再算一遍，和保存的结果比较。这里使用 SHA-512 作为 HMAC 内部的哈希算法，结果长度为 64 字节。

MAC 密钥负责认证，加密密钥负责解密。虽然两者有派生关系，但用途要分开。

这里验证时不仅涉及加密正文，还包括 IV 和页号。页号从 1 开始，按小端方式编码后参与计算。小端只是多字节数字的一种排列顺序，低位字节放在前面；顺序不对，算出来的 HMAC 就不同。页号参与计算，也让校验和页面所处的位置有关。

MAC 密钥来自原始加密密钥的进一步派生。旧实现先对数据库 salt 做固定变换，再和加密密钥一起送进 PBKDF2 派生算法，得到 32 字节 MAC 密钥。

具体参数是：salt 每个字节与 `0x3a` 做异或，PBKDF2 内部使用 HMAC-SHA512，迭代 2 次。异或是一种按二进制位进行的运算，这里用来变换 salt。理解流程时，先抓住“从已有密钥派生出另一把专门做认证的密钥”就够了。

这地方很容易看岔：**2 次迭代发生在“已有原始加密密钥，继续派生 MAC 密钥”这一步。** 不能据此说微信登录密码只做了两次哈希。这个工具走到这里时，根本就没在用登录密码做推导。

得到 MAC 密钥后，计算第一页的 HMAC，与文件里保存的值比较。对得上，才有理由继续按这套参数处理数据库。

### 第一页还要补回文件头

普通 SQLite 文件开头有一段固定标识：`SQLite format 3`，后面跟一个零字节，总共 16 字节。

而这个位置在加密文件中被 salt 占了。所以解密第一页时，需要把正常的 SQLite 文件头恢复回来。其他页不用补这个头。

页尾原来放 IV 和 HMAC 的 80 字节，在明文副本中可以补零。但是，页头中描述“保留区大小”的信息还得保持一致。

SQLite 用 B-tree 这类树形结构来组织表和索引，页面之间有对应关系，页面内部也有自己的布局。原来的数据库是按每页有 4016 字节可用空间来安排这些内容的。你把保留区声明改成 0，等于告诉 SQLite 一页有 4096 字节可用，可里面的数据根本没重排。这样很可能会把一个解密正确的文件，弄成结构错误。

所以解密并不是“把密文换成明文”就结束，文件格式也得一起还原好。

## 都解完了，为什么最新消息还没有

因为最新的修改不一定已经写进主数据库。

WAL 是 Write-Ahead Log 的缩写，也就是预写日志。SQLite 在这种模式下，会先把修改后的页面写到旁边的日志文件里，之后再合并回主文件。这个合并过程叫 checkpoint，通常译作“检查点”。

数据库还会把一组需要一起生效的修改组织成事务。“提交”表示这一组修改正式生效，不应该只生效一半。

这里有一点值得区分：**事务提交和 checkpoint 不是同一件事。** 消息可以已经提交、已经被微信正常读取，但页面还留在 WAL 里，没有合并回主数据库。

所以只解主文件，可能得到一份结构正常、但内容偏旧的数据库。看见缺了刚刚收到的消息，未必是密钥又失效了。

### WAL 存的是页面更新

在本文对应的格式里，WAL 开头有 32 字节文件头，后面是一组组 frame。Frame 可以理解成 WAL 中的一条页面更新记录，每条包含 24 字节的记录头，以及一页 4096 字节的数据。

frame 头里记录了页号。解密出这一页后，就能按页号，把它覆盖到明文副本的对应位置。

举个简单例子：主数据库里的第 5 页比较旧，而 WAL 里有它的新版本。那么处理完这份更新后，副本中的第 5 页就应该变成新页面。同一页可能更新多次，最终要采用有效事务范围内较新的版本。

### WAL 的 salt 和数据库 salt 不是一回事

WAL 文件会复用。一个周期结束以后，文件后面可能还留着旧 frame 的内容，不是文件里有字节，就全部都能拿来用。

WAL 文件头有两个 salt 值，frame 头中也有对应值。原项目通过比较它们，跳过不属于当前 WAL 周期的 frame。

这里的 salt 用于识别 WAL 周期，和前面匹配数据库、派生 MAC 密钥时用的 salt，用途不同。两个地方都叫 salt，容易看着看着混成一个东西。

另外，不能只靠文件大小判断是否有新消息。有的 WAL 会预分配或复用空间，长度没变，不代表内容没变。旧实现通过主文件和 WAL 的修改时间判断缓存是否需要刷新。

### 它还没有做到完整的事务校验

原项目有 WAL 处理，不等于它已经把 SQLite 的事务规则全部实现了一遍。

我保留的这版逻辑，会检查页号和 WAL salt，再把解密后的页面覆盖到副本里。但它没有完整验证 WAL checksum，也就是用于检查日志内容的校验和。这个校验和与前面带密钥的 HMAC 是两套机制。

它也没有严格按有效提交边界决定哪些 frame 可以应用。通俗点说，一组修改可能只写到一半，还没提交，不能看见页面就直接搬进最终副本。事务提交后数据库有多大，也需要按记录处理，旧逻辑在这方面同样不完整。

这意味着它对“读到最新页面”做了处理，但不能直接把结果当成严格一致的数据库快照。

特别是微信还在持续写入时，主数据库和 WAL 是分别读取的，读取中间又可能发生 checkpoint。两份文件各自都能读，不代表它们一定对应同一个时刻。

这些是继续做可靠性改进时要补的内容，不能因为平常能查到消息，就当作不存在。

## 解密成功，要分几层看

以前看工具输出，很容易看到“成功”就松口气。重新捋完这个过程，发现至少要分开看三个问题。

**第一，密钥和页面是否匹配。** 第一页 HMAC 通过，说明这一页在这组密钥和参数下验证成功。但旧版离线解密流程并没有给所有页面逐一做同样的 HMAC 校验，实时缓存那条路径也没有同样的第一页验证，不能一概而论。

**第二，SQLite 结构是否完整。** 能列出表名，只是说明相关结构读得出来。更完整的结构检查，还需要数据库自身的完整性检查机制，不能用“有表”代替“整库没有问题”。

**第三，消息是否齐全、是否够新。** 即使结构检查通过，也不能证明所有历史分库都在，更不能证明 WAL 中的最新消息已经正确合并。

密钥验证、结构验证、数据完整性，互相有关，但谁也不能替另外两项打包票。

原项目为了避免每次查询都重新解密，还会保存明文缓存。文件没变就复用，变了就重新解密对应数据库，再处理 WAL。它不是精确追踪“上次读到哪个 frame，这次只解后面几条”的实现。

这个区别也会影响性能。数据库越大，重新生成缓存的成本越高，不能看到工具写着“实时”，就默认它的开销和新消息数量成正比。

## 能打开数据库，离聊天记录还差一步

还原成普通 SQLite 以后，最难懂的部分基本过去了，但里面也不是一张现成的聊天列表。

一段私聊或一个群聊，在数据库中都有对应的会话标识。它通常是稳定的账号标识，不是会变化的昵称或群名。

消息表可能以这个会话标识的 MD5 命名。MD5 是一种哈希算法，能把输入转换成固定长度的值。原项目需要先从联系人、会话等信息中取得标识，再算出相应表名，建立对应关系。

这里的 MD5 只是参与表名生成，不是在“破解 MD5”。知道原始会话标识，计算出对应表名即可，不需要从哈希倒推群名。

接着还要处理发送者映射、时间、消息类型，以及正文内容。部分正文用了 Zstandard 这种压缩算法，需要先解压；引用、链接和文件等消息又可能使用 XML，一种通过标签组织字段的文本格式，还得按字段把内容拆出来。

所以完整顺序是：先解密数据库页，再由 SQLite 读取记录，最后按微信的数据结构还原成聊天内容。压缩、加密和消息解析是三个不同的问题，不能混着解决。

图片也一样。库里的消息和索引能读了，不代表本地图片文件就能直接打开。图片可能有另外的加密格式、密钥和资源定位方式，数据库的 key 不能直接拿来通吃。这里就不继续展开了，否则又要从数据库聊到一整套附件处理。

## 放回项目里看

Funtree 后来做的历史导入、断网补传、跨账号去重和后台查询，都建立在这层数据读取能力之上。

原项目解决的是怎样从本机的加密存储中取得可读数据，业务系统还得解决怎样保存和持续使用这些数据。比如 Mac 离线以后，已经同步的历史仍然能查；同一条消息重试上传，服务端不能重复记一遍。这些是后续接入的工作，不是 AES 解密的一部分。

我觉得这套实现最值得理解的地方，就是它把一个看起来很玄乎的“微信解密”，拆成了几个能具体讨论的问题：客户端什么时候持有密钥，怎么把密钥对应到数据库，一页数据怎么还原，最新页面又在哪里。

它也没有把 AES 算法攻破。磁盘上的加密和客户端运行时持有密钥，本来就同时存在。理解了这一点，再去看整个过程，就没那么神秘了。

不过明文副本一旦生成，就不能再依赖微信原来的数据库加密保护它。缓存、导出的记录和备份放在哪里，谁能读，仍然得自己管。尤其是要长期挂着用的时候，别只记得“已经能查了”。

## 参考资料

原项目名称保留用于说明本文来源，不提供已下架仓库的链接、源码或替代下载。

- [SQLCipher Design](https://www.zetetic.net/sqlcipher/design/)：页加密、密钥派生和 HMAC 的设计。
- [SQLite 数据库与 WAL 文件格式](https://www.sqlite.org/fileformat2.html#walformat)：页面、WAL frame、校验和提交标记。
- [SQLite WAL 模式](https://www.sqlite.org/wal.html)：事务提交、checkpoint 和并发读取。

上面的官方资料说明通用格式和设计；本文对旧工具行为及其不足的描述，来自我保留版本的实现阅读，二者不要混为一谈。
